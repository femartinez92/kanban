<h1>Listing tasks</h1>

 
       <ul>
    <% @tasks.each do |task| %>
        <li class="task" id="<%= task.id%>"> 
        <%= link_to task.title, task_path(task) %>  
        </li>

     <% end %> 
     </ul> 
     <br>

<%= link_to 'Nueva Tarea', new_task_path %>